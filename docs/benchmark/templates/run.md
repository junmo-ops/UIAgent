# 单次执行记录

复制一份对应一次尝试，不覆盖失败重跑。未知字段填写 N/A，不填 0。

## 身份与环境

- 批次ID / 任务ID / 尝试序号：
- 用例版本：v1
- 开始时间 / 结束时间 / 执行人：
- commit / 未提交改动补充记录：
- 插件实际版本 / 服务部署版本 / runtimeRevision：
- 模型供应方 / 模型名 / 接口版本 / 配置指纹（不含密钥）：
- 浏览器版本 / OS / DPR / 缩放：
- 捕获时内容视口 / 对照时内容视口 / 编辑时内容视口：
- 侧栏开关和宽度 / F12是否关闭 / 主题 / 根及子容器滚动位置：
- 资产ID / 资产版本或哈希：
- 是否新副本新会话；前置还原检查结果：
- workspaceId / conversationId / 全部turnId：
- 原文提示词、预设回复（或引用固定用例版本）：

## 结果

- 状态：not-run（执行后改为 pass / partial / fail / blocked）
- 首次按脚本完成：是 / 否 / 不适用
- 预设轮次 / 模型澄清次数 / 额外救援轮次：
- 实际额外输入（逐字保留，区分澄清与人工纠错）：
- 每项必需验收：逐条列出通过/未通过/未检查及证据
- 公共不变量：无关区域 / 布局 / 本地交互 / 刷新持久化
- 失败层：unknown
- 最早失败步骤：
- 实际结果与期望差异：
- 是否命中停止/重启等目标时机；未命中不得判通过：
- 观察到的事实：
- 根因假设（明确未证实）：

## 效率

- 采集耗时ms：
- 人工端到端耗时ms：
- 服务durationMs及对应日志ID：
- SDK calls数（不等价于HTTP次数）：
- 工具尝试 / 成功 / 失败 / 拦截：
- 输入Token / 输出Token / 缓存Token及口径：
- 用量完整性：完整 / 部分 / 缺失
- 金额与计价来源/日期（可N/A）：
- 终止原因：完成 / 用户停止 / 服务失败 / 到达本批预设观察上限
- 如达到上限，填写实际观察时长，不能记成完成耗时：

## 证据与复现

- 采集前原页截图：
- 初始副本截图：
- 修改后截图 / 交互录屏：
- 刷新后的截图：
- 全部日志（路由、编辑、重试等）：
- 基础及结果revision：
- 脱敏快照/模块源码（如允许）：
- 最短复现步骤：
- 与上一版本同任务的差异：
- 后续处置：修复 / 能力边界 / 环境准备 / 产品语义待确认

原始材料只放授权内部存储；本仓库 local/ 已忽略，但忽略规则不等于安全脱敏。
