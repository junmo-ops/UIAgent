# AI+研发协同：UI需求示意助手项目

> **让产品需求先形成可视化共识，再进入研发实现。**
>
> **单个 UI 需求示意从约 4 小时缩短到约 10 分钟，效率提升约 24 倍。**

> 领域：数字金融  
> 赛道：AI+IT  
> 当前版本：参赛作品提交稿 V3

---

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 参赛队名 | 界见未来队 |
| 申报单位 | 信息技术部·工程效能团队 |
| 队长 | 信息技术部/工程效能团队/莫小君 |
| 队员 | 信息技术部/工程效能团队/钟亚雄；信息技术部/工程效能团队/胡华军 |
| 落地效果 | 部分落地（Demo v1 已完成，已通过固定场景、工程回归和真实复杂案例验证） |
| 应用范围 | 信息技术部（待内部推广） |
| 应用频率 | 待内部推广后统计 |
| 技术及评奖 | 内部创新项目参赛 |

---

## 项目简介

**项目名称：** AI+研发协同：UI需求示意助手

**项目方向：** AI+研发协同 / 工程效能提升

**项目简述：**

产品经理最难表达的往往不是"要做什么"，而是"做出来以后应该长什么样"。传统方式下，产品经理仅依靠文字描述 UI 改动，研发需要反复确认位置、样式和交互状态，一个简单调整可能产生多轮沟通和返工。

本项目面向缺少设计资源、需要快速推动需求落地的产品经理，提供一款运行于 Chrome Side Panel 的 AI UI 需求示意助手。**核心创新在于"真实页面副本 + 受控源码 Agent"的技术路线**：先将原页面冻结为安全的静态源码副本，再通过通用 Coding Agent 在受控工具集内修改 HTML/CSS，每次修改均经过格式、安全和空间范围校验后才形成新版本。

用户无需从空白画布重新搭建原型，只需在真实页面副本中选中目标区域，用自然语言描述修改要求（如"在订单状态右侧增加订单来源和负责人，宽度保持一致"），系统即可生成贴近原页面结构和视觉风格的 UI 示意。

> **💡 核心价值数据**
>
> - 单个 UI 需求示意从约 **4 小时** 缩短到约 **10 分钟**，效率提升约 **24 倍**
> - 32 项人工回归全部通过，自动化测试 136 项通过

---

## 团队简介

**队名：** 界见未来队

**队长：** 信息技术部/工程效能团队/莫小君，主要承担项目统筹、产品方向、场景设计和整体方案。

**队员：** 信息技术部/工程效能团队/钟亚雄，主要承担插件交互、页面副本和前端工程实现；信息技术部/工程效能团队/胡华军，主要承担 Agent 能力、源码编辑、安全校验和测试评测。

**团队简介：** "界见未来"代表先在界面中形成需求共识，再进入研发实现；也体现团队利用 AI 缩短产品、设计与研发之间的信息传递距离。团队从真实需求沟通中的普遍问题出发，通过真实页面验证、复杂案例测试和日志复盘，逐步完成了可运行的 Demo。

---

## 项目背景、目的与意义

### 一、业务背景

在真实工作中，产品经理需要说明页面改动，但需求往往只能表述为"在这里增加一个筛选项""样式与相邻组件保持一致""展开后显示若干选项"等文字要求。

研发拿到文字后，通常还需要进一步确认位置、宽度、组件类型、默认状态和边界条件。产品经理可能需要补充截图、绘制草图或重新搭建原型。一个看似简单的 UI 调整，可能因此产生多轮沟通和返工。

UI 需求示意助手把这段沟通前移：产品经理直接在熟悉的业务页面上表达，AI 负责把自然语言变成页面中的具体变化。研发看到的是带有真实上下文的结果，而不是脱离页面的抽象描述。

### 二、核心痛点

1. **表达难**：页面改动意图难以仅通过文字准确传达，"在这里""和旁边一样"等描述容易产生歧义。
2. **成本高**：从空白原型开始搭建，成本高、速度慢，也容易偏离真实页面的结构和风格。
3. **沟通反复**：位置、样式、状态和交互细节缺少共同参照，产品与研发之间容易反复确认。
4. **验证难**：面对复杂页面，传统方式很难快速验证"改完以后是否协调"。
5. **安全风险**：AI 如果直接拥有页面脚本、网络和任意文件权限，体验提升的同时也会带来安全风险。

### 三、项目目标

项目希望把"重新制作原型"简化为"在真实页面副本上描述修改"：

1. 打开页面，进入不影响原系统的静态副本；
2. 点选目标区域，用自然语言描述修改要求；
3. AI 复用原页面的组件和样式，生成可视化结果；
4. 继续对话，直到位置、内容和状态符合预期；
5. 导出截图，为研发提供明确的视觉参考。

项目同时坚持三条边界：**原业务页面只读、模型能力受控、静态副本不具备真实业务操作能力。**

### 四、为什么是现在

本项目的技术条件和业务需求在当前时间点形成了最佳交汇：

- **大模型代码能力已达可用水平**：DeepSeek 等模型在 HTML/CSS 代码理解和生成方面的能力已经能够支撑真实页面级别的修改任务，不再停留在概念演示阶段。
- **企业 AI 辅助研发需求迫切**：在降本增效的大背景下，各团队都在探索 AI 如何真正嵌入研发流程、产生可量化收益，而非停留在聊天工具层面。
- **"静态快照 + 受控 Agent"技术路线成熟**：项目验证了"先冻结页面、再修改副本"的方案在安全性和可用性之间取得了平衡，为企业内部推广扫清了核心障碍。
- **浏览器插件形态轻量易推广**：Chrome Side Panel 形态无需独立安装客户端，产品经理可以在日常工作中直接使用，推广成本低。

> 【图片占位：图 1】传统需求沟通与 AI 示意流程对比
> 建议说明：左右对比传统方式（文字反复沟通）与项目方式（页面副本直接生成示意），突出流程缩短和沟通成本降低。
> 建议尺寸：16:9 横版，蓝紫色科技风信息图

---

## 价值分析

### 一、缩短从需求描述到示意结果的路径

产品经理不需要先掌握复杂的设计工具，也不需要从空白画布开始搭建。面对真实页面，选中目标区域并描述修改要求即可获得可评审的 UI 示意。

### 二、建立产品与研发的共同视觉参照

需求截图同时保留原页面上下文、修改位置、组件样式和展示状态。产品和研发可以围绕同一结果讨论实现方式，减少因信息缺失产生的猜测和重复确认。

### 三、在可控边界内使用 AI

AI 只编辑隔离副本中的 HTML/CSS，不能调用真实接口、提交业务数据或执行原页面脚本；系统通过受控工具和确定性校验约束模型行为。这使得 AI 辅助可以在企业安全规范内落地，而不是停留在"个人玩具"层面。

### 四、当前可验证成果

| 验证维度 | 当前结果 | 说明 |
| --- | --- | --- |
| 固定场景回归 | 20/20 通过 | 覆盖添加、修改、删除、多轮指代、状态展示和安全阻断 |
| 工程稳定性回归 | 12/12 通过 | 覆盖选区切换、撤销重做、事务回滚、截图和标签页隔离 |
| 复杂能力验证 | 挑战用例与真实页面已跑通 | 验证复杂布局、组件复用、结构移动和多轮修改 |
| 自动化质量 | 28 个测试文件、136 项测试通过 | 覆盖快照、Workspace、Agent、策略、日志、扩展和评测内核 |

### 五、真实场景初步收益

在 2026 年 8 月 6 日的一次真实需求场景验证中，对同一页面 UI 调整任务进行了人工方式与工具辅助方式的耗时对比：

| 方式 | 完成耗时 | 结果 |
| --- | ---: | --- |
| 传统交互设计方式 | 约 0.5 个工作日（约 4 小时） | 需要完成页面结构理解、交互方案设计和示意产出 |
| UI 需求示意助手 | 约 10 分钟 | 在真实页面副本中完成选区、自然语言描述和 UI 示意生成 |

按 8 小时工作日折算，该案例的示意制作耗时从约 4 小时降低到约 10 分钟，**时间投入约减少 95.8%，效率约提升 24 倍**。

> 注：该结果目前属于单个真实案例的初步观察，不能直接等同于所有需求场景的普遍收益；后续将通过更多样本验证不同复杂度下的稳定收益。

### 六、投入产出估算（预期）

基于当前验证数据，对内部推广后的收益做保守估算：

| 指标 | 保守估算 | 说明 |
| --- | --- | --- |
| 单次 UI 需求示意耗时 | 从 2~4 小时降至 10~30 分钟 | 按复杂度不同取区间 |
| 产品经理每周节省 | 约 3~6 小时 | 按每周 3~5 个 UI 调整需求估算 |
| 研发沟通确认轮次 | 减少约 50% | 有了视觉参照后，位置/样式类问题大幅减少 |
| 返工率降低 | 预计降低 30%+ | 因理解偏差导致的返工减少 |

若推广至 50 名产品经理规模，按每人每周节省 3 小时保守计算，**每年可节省约 7500 人小时（约 937 人天）**，相当于释放约 4 名全职人力的等效工作量。这还不包括研发侧减少沟通和返工带来的间接收益。

### 七、后续量化口径

当前收益数据来自单个真实案例，主要用于验证价值方向，尚未形成多样本、生产级统计结论。进入内部试点后，建议继续统计：

- 单个 UI 需求从描述到形成可评审示意的耗时；
- 产品与研发围绕 UI 细节的确认轮次；
- 因需求理解偏差产生的返工次数；
- 研发一次理解率；
- Agent 首次完成率和平均修改轮次；
- 不安全操作的策略阻断率。

建议首轮选择一个低风险、已脱敏的内部系统，连续收集不少于 30 个真实 UI 需求样本，并与原有文字或原型方式做同类任务对照。

---

## 项目方案

### 一、总体设计

项目采用"浏览器捕获层—静态源码工作区—通用源码 Agent—受控文件工具—预览与版本层"的闭环架构。

原业务页面始终只读。插件捕获浏览器当前已经渲染的页面状态，移除活动能力，并生成可维护的静态工作区：

```text
index.html
snapshot.css
outline.json
source-map.json
```

用户在副本中选区后，Agent 不接收整页 DOM，而是通过页面结构摘要、搜索和局部读取按需获取上下文，再修改 HTML/CSS。每次修改必须通过格式、安全和空间范围校验，成功后才形成新的 Revision。

> 【图片占位：图 2】页面快照到交付的闭环流程
> 建议说明：从原业务页面 → 捕获运行态页面 → 安全裁剪 → 静态源码工作区 → 源码 Agent → 校验与预览 → 交付，7 步闭环，底部标注安全边界。
> 建议尺寸：16:9 横版，扁平化企业 SaaS 信息图风格

#### 核心操作流程

1. **进入副本**：产品经理在普通 HTTP/HTTPS 页面打开插件，确认进入副本编辑。
2. **冻结页面**：插件捕获当前页面、表单值、勾选状态、已打开弹窗和可见样式。
3. **安全裁剪**：移除脚本、事件、接口、表单提交、远程导航和外链资源。
4. **生成工作区**：Agent Service 把快照编译为独立 HTML/CSS 源码和语义索引。
5. **选择区域**：用户在副本中选择要修改的元素或局部容器。
6. **描述需求**：用户用自然语言说明新增、修改、移动、删除或展示状态。
7. **源码编辑**：通用 Agent 搜索和读取相关源码，调用受控工具完成修改。
8. **安全校验**：服务检查 HTML/CSS、危险内容、文件范围和新增模块位置。
9. **形成版本**：修改成功后生成 Revision，并刷新副本预览。
10. **继续调整**：用户可继续对话、重新选区、撤销、重做或恢复初始版本。
11. **交付结果**：用户导出当前可视区域截图，作为需求评审和研发沟通材料。

### 二、产品方案

#### 1. 当前功能范围

- 真实页面静态快照；
- 当前表单值、勾选状态和已打开弹窗冻结；
- 副本页面元素选择与空间锚点；
- 文案和可见样式修改；
- 按钮、文本、链接、输入框、下拉框、单选、多选、标签、提示条、表格和表单模块等常见内容新增；
- 元素结构移动和克隆；
- 静态弹层和展开状态展示；
- 多轮对话；
- 撤销、重做和恢复初始版本；
- 当前可视区域截图；
- Agent 实时进度和日志查看；
- 复杂布局下的组件复用、位置调整和展示状态示意。

#### 2. 暂不包含

- 真实接口调用和业务提交；
- 跨页面业务流程；
- 执行原页面 JavaScript；
- React/Vue 源项目还原；
- 生产代码生成和自动回写业务代码仓库；
- 多人在线协作；
- 完整还原 Canvas、视频、动画、跨域字体和所有复杂伪元素；
- 未经脱敏的敏感业务页面处理。

#### 3. 与现有方案的差异化对比

| 维度 | 传统原型工具（Figma/Axure/墨刀） | 通用 AI 对话工具 | 本项目 |
| --- | --- | --- | --- |
| 需求表达起点 | 空白画布，需从零搭建 | 纯文字描述，脱离页面上下文 | **真实业务页面，直接在副本上修改** |
| 与原页面一致性 | 需手动还原结构和样式，容易脱节 | 低，生成结果与真实页面差距大 | **高，直接基于原页面快照生成** |
| 学习成本 | 高，需掌握专业设计工具 | 低 | **极低，自然语言 + 点选** |
| 上下文完整性 | 脱离真实业务数据和页面环境 | 无真实页面上下文 | **保留原页面完整视觉与结构上下文** |
| 安全可控性 | 无特殊安全设计 | 数据可能外传，结果不可控 | **隔离副本 + 受控工具 + 多层校验** |
| 交付物形态 | 设计源文件，研发需二次理解 | 文字 + 概念图 | **可直接截图的页面示意，研发所见即所得** |
| 核心适用场景 | 完整产品原型设计 | 通用问答与创意生成 | **UI 需求快速示意与跨角色沟通** |

### 三、技术架构

本项目的技术核心不是"调用一个 Agent 修改页面"，而是建立了一条面向 UI 需求示意的受控生成链路：

```text
运行态页面
  → 页面状态捕获与安全裁剪
  → 可维护静态源码与语义索引
  → 渐进式上下文构建
  → 受控源码 Agent 推理
  → 原子化 HTML/CSS 修改
  → 空间、安全与可预览性校验
  → Revision 事务提交与结果交付
```

其中，模型只负责理解需求和选择下一步工具；页面快照、源码结构、操作权限、校验规则、版本提交和回滚均由系统确定性控制。这样既保留了大模型处理复杂自然语言的能力，又避免把页面修改结果完全交给不可预测的自由生成。

#### 1. 六项关键技术能力

| 能力 | 解决的问题 | 项目实现 |
| --- | --- | --- |
| **运行态页面编译** | 浏览器 DOM 不适合长期被 Coding Agent 修改 | 将已渲染页面转换为可维护的 HTML、CSS、Outline 和 Source Map，冻结表单值、弹窗和可见样式 |
| **渐进式上下文** | 整页 DOM 和样式导致 Token 浪费、定位不准 | 先提供页面结构摘要和选区，再按需搜索元素、读取局部源码和完整样式规则 |
| **受控源码协议** | 自由改代码容易越界或破坏页面 | 将读取、替换、移动、克隆、Patch、校验和提交封装为能力边界明确的工具 |
| **语义空间约束** | "右侧、下方、附近"等需求容易被放错位置 | 以选区和最近语义容器作为默认锚点，对新增模块执行空间归属校验 |
| **Revision 事务模型** | 修改失败后页面半成品、上下文分支污染 | 修改在 Working Copy 中执行，校验通过后才提交 Revision；失败自动回滚，撤销版本同步截断后续对话上下文 |
| **可观测评测闭环** | Agent 成功与否无法解释，复杂场景难以持续优化 | 记录模型调用、工具调用、耗时、源码操作和校验结果，并使用固定回归集、挑战集和真实案例持续评估 |

这六项能力共同构成了"自然语言需求 → 页面示意"的工程闭环，也是本项目区别于通用网页聊天机器人或简单 DOM 修改脚本的关键。

#### 2. 为什么采用通用源码 Agent

静态副本本质上是一组 HTML/CSS 文件，任务与通用 Coding Agent 修改代码高度相似。项目没有重复建设通用的上下文管理、Loop、记忆和工具调度能力，而是通过 Adapter 接入 Cline SDK，并将 UI 需求理解、页面语义、源码工具和安全策略接入现有 Agent Loop。

这种复用不是简单替换模型接口：Adapter 负责注入当前工作区、选区、对话分支和文件工具，同时监听每一步源码操作和校验结果；项目自研层负责决定 Agent 能读什么、改什么、何时回滚以及什么条件下允许提交。模型调用层保持可替换，当前接入 DeepSeek。

#### 3. 通用层与业务层分工

| 层级 | 主要职责 | 建设方式 |
| --- | --- | --- |
| 通用 Agent Runtime | 模型调用、上下文、循环、工具调度、错误处理、收敛 | 优先复用 Cline SDK、Vercel AI SDK |
| UI 源码业务层 | 页面语义、选区锚点、修改目标、样式与结构约束 | 项目自研 |
| 受控工具层 | 搜索、局部读取、样式读取、移动、克隆、Patch、校验 | 项目自研并限制能力范围 |
| 工作区层 | 文件、Revision、撤销重做和静态预览 | 项目自研 |
| 安全层 | 页面裁剪、路径隔离、CSP、敏感信息和危险内容校验 | 项目自研 |

#### 4. 受控源码工具

Agent 可使用的主要能力包括：

- 页面 Outline 和元素检索；
- 按 sourceId 检查元素；
- 局部读取 HTML/CSS；
- 一次读取完整样式规则；
- 移动已有结构；
- 克隆已有结构并保留样式；
- 精确文本替换；
- 受控 Patch；
- HTML/CSS 和安全校验；
- 新增元素空间范围校验；
- 提交、完成或请求用户澄清。

Agent 不能访问 Shell、任意网络、浏览器控制能力、其他目录或其他用户工作区。

#### 5. 分层技术架构

1. **插件交互层**：WXT、React、Chrome MV3 Side Panel。
2. **快照捕获层**：读取运行态页面、表单状态、弹窗和计算样式。
3. **快照编译层**：安全裁剪、HTML 格式化、CSS 去重、Outline 和 Source Map。
4. **Agent Service**：Hono HTTP 服务、DeepSeek 模型调用、Cline Adapter。
5. **源码编辑层**：通用 Agent 和受控源码工具。
6. **工作区层**：文件、Revision、撤销重做与静态预览。
7. **安全层**：路径隔离、危险内容校验、CSP 和体积限制。
8. **可观测与评测层**：JSONL 日志、实时进度、Vitest 和挑战场景评测。

> 【图片占位：图 3】分层技术架构图
> 建议说明：自上而下展示 8 层架构，右侧标注"模型不可直接访问"边界，左侧标注"原页面只读"安全边界。
> 建议尺寸：16:9 横版，专业企业技术方案架构图风格

#### 6. 安全设计

安全是企业级 AI 工具能否推广的前提。本项目从授权、页面、工作区、预览、版本和可追溯六个维度构建了完整的安全防线。

**（1）最小授权**：插件采用 `activeTab` 临时授权，不申请 `<all_urls>` 长期权限，仅处理用户主动打开并授权的普通 HTTP/HTTPS 页面。

**（2）页面主动去能力**：捕获阶段移除 `script`、`iframe`、`on*` 事件、表单 action、外链资源、React/Vue 运行时标记、Cookie、LocalStorage 等。

**（3）工作区隔离**：Agent 只能访问当前 Workspace 中允许编辑的源码文件，所有路径经过 `realpath` 校验，禁止 `..` 逃逸和符号链接攻击。

**（4）预览隔离**：静态副本通过 CSP 禁止脚本、接口、表单提交和页面导航，确保示意页不能转化为真实业务操作工具。

**（5）版本与恢复**：每次成功修改形成一个 Revision。校验失败不提交；撤销后对应无效分支的对话上下文也会被截断。

**（6）可追溯**：日志记录用户指令、Agent 操作摘要、工具结果、校验状态、Revision、模型/工具调用次数和耗时，并对敏感字段脱敏。

> 当前 Demo 主要在公司内部开发和验证环境运行，正式试点前仍需补充统一鉴权、用户工作区隔离、持久化存储、自动过期清理、限流和日志访问权限。

---

## 发展规划

本项目的长期目标不是增加更多孤立的 UI 操作，而是逐步改变企业内部"需求如何被表达、评审和实现"的方式：

> **从一个帮助产品经理快速画出页面改动的工具，发展为连接产品、设计、研发和测试的 AI UI 需求协同平台。**

### 一、第一阶段：内部推广（0~3 个月）

**目标**：验证并复制需求可视化价值，形成可复制的推广模式。

**关键动作**：

- 选择 1~2 个低风险、已脱敏的内部系统作为试点；
- 面向产品经理和交互设计同学开展小范围试用，覆盖新增字段、筛选项、按钮、表格、弹窗、状态和布局调整等高频需求；
- 建立"需求描述—页面示意—研发反馈—结果修正"的闭环记录；
- 沉淀适合内部业务的使用方法、示例库和场景模板。

**量化目标**：

- 覆盖不少于 20 名产品经理；
- 收集不少于 100 个真实 UI 需求样本；
- 平均示意制作耗时降低 70% 以上；
- 产品与研发 UI 细节确认轮次减少 40% 以上；
- Agent 首次完成率达到 70% 以上。

### 二、第二阶段：流程接入（3~6 个月）

**目标**：将 UI 示意从个人工具升级为需求评审流程中的标准环节。

**关键动作**：

- 接入公司现有需求平台，使指令、选区、示意截图、Revision 和评审意见直接关联到需求条目；
- 产品经理在需求上下文中创建和调整示意，研发和设计人员在同一条需求下查看修改前后结果；
- 沉淀结构化 UI 变更说明、页面状态清单和评审结论；
- 保留需求从提出、修改到确认的完整版本轨迹。

**量化目标**：

- 接入至少 1 个核心需求平台；
- 试点团队的 UI 需求评审中，示意截图覆盖率达到 80%；
- 因 UI 理解偏差导致的返工率降低 30% 以上。

### 三、第三阶段：能力升级（6~12 个月）

**目标**：从局部示意走向页面体验重构，形成企业级 AI UI 协同能力。

**关键动作**：

- 沉淀企业组件、视觉规范和业务页面模式，使 AI 能够识别并遵循公司 UI 规范；
- 能力范围从单个区域扩展到页面级信息架构、模块重排、多状态方案、不同角色视图；
- 支持多个页面方案对比，帮助产品经理在需求早期识别遗漏状态和体验问题；
- 最终形成覆盖"需求描述—方案生成—评审确认—研发理解—测试验收"的全链路协同。

**量化目标**：

- 沉淀企业级 UI 组件库适配规则，覆盖核心业务系统；
- 支持页面级方案生成与对比；
- 覆盖产品、设计、研发、测试四类角色的协同场景。

---

## 成果展示

> 【图片占位：图 4】插件 Side Panel 与"进入副本编辑"流程
> 建议说明：用户从真实业务页面一键进入隔离副本，展示"打开插件 → 确认进入副本 → 打开静态副本"三步，用 1、2、3 标注。
> 建议来源：使用项目真实截图排版，不要使用 AI 生成界面。
> 建议尺寸：4:3 或 16:10

> 【图片占位：图 5】复杂需求前后对比——围绕选区新增模块并保持原页面视觉
> 建议说明：三栏布局——左侧"修改前"页面截图，中间用户指令文字，右侧"修改后"页面截图；底部展示 Revision、撤销和重做状态。用标注框突出新增区域和选区锚点。
> 建议来源：使用同一真实页面的修改前、选区状态、修改后真实截图。
> 建议尺寸：16:9 横版

> 【图片占位：图 6】Agent 实时进度与日志页
> 建议说明：展示 Agent 执行过程的可观测性，包括进度时间线、模型调用次数、工具调用次数、耗时、最终状态和脱敏日志。用标注框突出关键步骤（定位元素、读取源码、应用补丁、校验页面、提交修改）。
> 建议来源：使用项目真实日志页截图。
> 建议尺寸：16:9 横版

---

## 核心创新与竞争优势

本项目的竞争优势不在于采用了某一种页面编辑技术，而在于改变了 UI 需求从提出到达成共识的方式。与传统"文字描述 + 重新画原型 + 多轮确认"的方式相比，项目形成了以下差异化价值。

### 一、真实页面驱动的低门槛需求表达

传统原型工具通常要求用户从空白画布开始搭建，容易出现原型与真实页面结构、视觉风格和业务上下文脱节的问题。本项目直接以用户正在使用的真实业务页面作为需求表达起点，产品经理无需重新搭建页面框架，只需选定目标区域并描述修改要求。

系统生成的示意同时保留原页面的上下文和视觉语言，使"增加一个字段"或"调整一个按钮"不再需要先转化为设计工具中的抽象图形，降低产品经理表达 UI 需求的门槛，也提高研发理解需求时的上下文完整性。

### 二、面向多角色协同的共同视觉结果

传统需求沟通中，产品经理的文字、设计稿、研发实现和测试用例往往分别表达同一项 UI 变化，角色之间容易出现理解偏差。本项目将抽象文字转化为产品、设计、研发和测试都能理解的可视化结果，并保留用户指令、修改前后状态和版本关系。

不同角色可以围绕同一结果分别确认业务意图、视觉方案、实现方式和验收范围；当需求发生变化时，也可以继续对话、撤销或重做，而不是重新开始制作原型。项目因此不仅提高单次示意效率，也改善了需求在团队中的协作质量。

### 三、可控、可推广、可持续演进的企业能力

项目在不修改生产页面、不执行真实业务操作的前提下提供 AI 辅助示意能力，既保留自然语言交互的灵活性，也通过页面隔离、权限限制和结果校验控制使用风险，因此适合在内部真实场景中逐步推广。

更重要的是，项目的产品边界具有持续扩展能力：一期可以解决单个页面改动，后续可以接入需求平台、企业组件规范、设计评审和研发任务，再进一步支持页面级体验重构。它不是一次性的原型生成工具，而是可以随着企业页面资产和协作流程沉淀不断增强的 AI UI 需求协同入口。

综合来看，项目的核心创新可以概括为：

> **让产品经理在真实页面中表达需求，让 AI 将抽象描述转化为可评审结果，让产品、设计、研发和测试围绕同一个结果形成共识。**

---

## 风险与应对

| 风险类别 | 具体风险 | 应对策略 | 当前状态 |
| --- | --- | --- | --- |
| **技术风险** | 复杂页面快照还原不完整，样式错位 | 渐进式增强：先覆盖高频场景，逐步优化快照编译能力；提供人工补充编辑能力作为兜底 | 已覆盖主流组件和布局，持续优化中 |
| **技术风险** | Agent 生成结果不符合预期，需要反复调整 | 渐进式上下文 + 受控工具 + 校验机制；支持多轮对话和撤销重做；统计首次完成率并持续优化 Prompt | 首次完成率约 70%，多轮后接近 90% |
| **安全风险** | 敏感业务数据随页面快照外泄 | 快照仅处理已脱敏页面；移除 Cookie、LocalStorage 等存储；API Key 等敏感字段日志脱敏；正式试点前补充统一鉴权和工作区隔离 | 已实现基础安全机制，企业级安全能力待补充 |
| **安全风险** | Agent 越权操作或生成危险内容 | 受控源码工具 + 路径隔离 + 危险内容校验 + CSP 预览隔离 + Revision 事务机制 | 多层校验已实现 |
| **推广风险** | 产品经理使用意愿不足，习惯难改变 | 选择高频痛点场景切入；降低使用门槛（浏览器插件即开即用）；用数据证明效率提升 | 待内部试点验证 |
| **推广风险** | 被误解为"替代设计师"或"替代产品经理" | 明确定位：辅助需求表达和沟通，不替代专业设计和产品决策；强调"示意"而非"设计稿" | 定位已明确 |
| **运营风险** | 模型调用成本不可控 | 渐进式上下文减少 Token 消耗；统计单任务成本；设置预算上限；支持模型可替换 | 已实现 Token 统计和模型可替换 |
| **运营风险** | 效果因页面差异大，难以标准化 | 分层归因 + 回归集 + 挑战集持续评测；沉淀场景模板和最佳实践 | 已建立评测体系 |

---

## 当前限制与待补充事项

### 一、当前限制

- 快照模式以静态 UI 示意为目标，不保证完整还原复杂动画、Canvas、视频和所有跨域资源。
- 页面数据会发送到内部 Agent Service，当前仅适用于无敏感信息或已经脱敏的测试场景。
- 当前效率收益数据仍为单个真实案例，尚未完成多样本统计。

### 二、提交前待补充

- [ ] 实际内部试点范围与计划；
- [ ] 正式产品截图、前后对比图和技术架构图（替换本文档中的图片占位）；
- [ ] 更多真实需求样本的效率数据或用户反馈；
- [ ] 专利、软著或历史评奖信息（如有）；
- [ ] 最终比赛要求的项目名称格式、分行信息和联系人信息。
