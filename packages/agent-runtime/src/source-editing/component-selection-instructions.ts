export const COMPONENT_SELECTION_INSTRUCTIONS = [
  '组件选型由模型理解用户语义，不按关键词、站点或固定 DOM 分支。首次写入前，在 declare_intent.summary 中简洁记录三个维度及实现选择：操作意图（新建/复制/修改/重做）、视觉要求（默认 Ant Design/沿用所在区域/参考指定模块/精确复制）、交互要求（静态展示/本地交互/真实业务行为），并说明关键依据。',
  '新建模块（包括单控件）且没有明确风格复用要求时，统一使用 ui-agent-module 的 Ant Design 默认风格；只读取插入位置、可用宽度、父容器布局、overflow 等放置证据，不搜索原站组件库、主题或控件内部样式。对齐、尺寸协调、间距一致本身属于布局要求，不自动视为复刻主题。',
  '用户明确要求与页面风格一致或复用指定模块样式时，读取相关区域的结构和样式，由模型决定复用现有 HTML/CSS 或采用系统组件。参考范围优先用户指定模块，其次选区所在区域；多种风格导致显著不同结果且参照不明时先 clarify。取得足够证据后停止检索，不遍历全页。',
  '明确复制或要求一模一样时，优先复用目标结构、样式及已有实现；ui-agent-module 复制并按需调整其 module.js 代码，普通 HTML 复制相关结构和样式。参考样式但更换内容不等于复制全部数据和交互。修改或重做已有模块不能仅因新增 DOM 较多就归类为新建，未要求改风格时延续原有视觉约束。',
  '多轮任务以最新明确要求为准，保留未被覆盖的约束。用户同时要求指定组件库和精确复刻时，先判断能否同时满足；存在实质冲突才 clarify 优先级。不得修改React 管理的运行时 DOM 或靠原站 class 假装恢复组件交互。',
  '分别检查视觉与交互是否可实现。复制静态结构不恢复原站业务脚本；当前未支持的组件、日期范围、表格交互等不能用相似外观冒充功能。若所选实现无法满足明确需求，说明具体缺口并 clarify 替代或缩减范围，不静默降级。真实业务请求不在副本能力内。所有实现均须检查放置空间、弹层裁切和溢出；只有重大歧义、冲突或能力缺口才提问。'
].join('\n');
