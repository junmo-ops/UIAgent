export const COMPONENT_SELECTION_INSTRUCTIONS = [
  '组件选型由模型理解用户语义，不按关键词、站点或固定 DOM 分支。首次写入前，在 declare_intent.summary 中简洁记录三个维度及实现选择：操作意图（新建/复制/修改/重做）、视觉要求（默认 Ant Design/沿用所在区域/参考指定模块/精确复制）、交互要求（静态展示/本地交互/真实业务行为），并说明关键依据。',
  '实现方式按实际改动范围确定：明确复制既有区域并保留原样时复用原结构；仅改文案、颜色、间距、属性等且不改变控件类型或交互结构时修改现有实现；新增控件或模块、重做控件、改变控件类型或组合交互时，必须使用 ui-agent-module 中真正的 Ant Design 组件。不得用原生 select、手写下拉或原站组件 class 替代新增或重做的 Ant Design 控件。',
  '混合任务按各部分判断，不因插入位置属于已有页面就把新增控件归类为既有内容小改。需要共同布局或联动的新控件与重做控件，可组合为一个局部 React 模块；仅将完成需求所需的局部范围交给模块，不重建无关区域。由实际结构决定宿主位置与周边布局，不能按文案或固定 DOM 层级决定包装方式。',
  'Ant Design 模块默认使用库内风格，只读取插入位置、可用空间、父容器布局、overflow 和必要相邻元素；不检索原站控件内部样式。明确要求风格一致时，仅读取必要参照，通过 Ant Design 公开属性、主题配置或模块局部样式协调，不因此切回原生控件或手写组件。',
  '明确复制并保留原样时，普通 HTML 复用原结构和样式，ui-agent-module 复用并按需调整 module.jsx；复制静态结构不恢复原站业务脚本。单纯要求颜色、尺寸或间距一致不等于精确复制。',
  '用户澄清已确认的位置、范围和交互作为本轮约束，不重新比较已排除方案。取得必要证据后，用 declare_intent 简短记录实施选择并执行；没有新的冲突证据时不反复重新选型。',
  '多轮任务以最新明确要求为准，保留未被覆盖的约束。用户同时要求指定组件库和精确复刻时，先判断能否同时满足；存在实质冲突才 clarify 优先级。不得修改React 管理的运行时 DOM 或靠原站 class 假装恢复组件交互。',
  '分别检查视觉与交互是否可实现。复制静态结构不恢复原站业务脚本；Ant Design 公开组件与 React 本地状态可用于演示交互，不沿用早期单控件试点的能力限制；实际无法实现的行为不能用相似外观冒充功能。若所选实现无法满足明确需求，说明具体缺口并 clarify 替代或缩减范围，不静默降级。真实业务请求不在副本能力内。所有实现均须检查放置空间、弹层裁切和溢出；只有重大歧义、冲突或能力缺口才提问。'
].join('\n');
