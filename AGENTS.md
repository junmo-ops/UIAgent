# UIAgent 工程原则

## 验证协作约定

- 按用户要求，后续改动不新增、不运行单元测试，除非用户另行明确要求。
- 功能与页面效果由用户在真实场景中验证；可进行必要的类型检查和构建打包，不以这些检查声称视觉效果已验证。

## 通用方案优先

本项目面向任意网页形态。所有产品能力、Agent 策略、源码工具和校验机制都必须设计为可泛化方案，禁止针对单个网站、页面、组件、文案或测试 Case 做特殊兼容。

### 禁止的实现方式

- 根据某几个自然语言关键词硬编码具体布局决策。
- 根据固定 `sourceId`、业务 class、页面文案、站点 URL 或组件名称分支处理。
- 假设固定的 DOM 层级、祖先数量、flex/grid 结构、卡片列数或容器尺寸。
- 为通过单个截图或测试 Case 增加不可泛化的 CSS、选择器或位置修正规则。
- 用启发式 DOM `before/after` 代替真实的视觉位置理解。
- 在没有渲染证据的情况下声称样式、位置、可见性或布局已经正确。

### 推荐的能力闭环

1. 读取结构化页面、布局和样式上下文。
2. 由模型形成结构化需求意图，并识别会显著影响结果的歧义。
3. 存在歧义时向用户澄清；意图确认前禁止修改源码。
4. 由模型根据当前页面实际结构生成修改计划，不由业务代码猜测具体布局策略。
5. 通过受控、通用的源码工具执行修改。
6. 在真实浏览器渲染后验证位置关系、重叠、裁切、溢出、可见性和样式一致性。
7. 将通用验证结果反馈给模型迭代；验证通过后才能提交 Revision。

确定性代码负责协议、安全边界、状态机和可观测事实；大模型负责语义理解、歧义判断与页面相关的布局决策。

### 设计审查要求

- 如果一个改动只能用当前示例解释，而无法说明对其他网页形态为何成立，应停止实现并重新抽象。
- 新能力至少使用多种不同布局形态验证，例如普通流、flex、grid、定位容器和可滚动区域。
- 回归测试应验证通用不变量，不应只复刻某个业务页面的 DOM 和文案。

### 已纠正的错误方向

此前围绕“复制模块并放在下面”加入的自然语言正则门禁、澄清选项到固定 `parentEnd` 的映射、固定放宽两级祖先高度，以及对普通容器统一改为 `height:auto`，都属于启发式 Case 处理，现已移除。当前采用“结构化意图 + 布局上下文 + 模型规划”的通用流程；真实浏览器几何验证仍应作为后续闭环能力继续建设。
